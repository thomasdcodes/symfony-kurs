{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-end">
        <button type="submit" class="btn btn-outline-success">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
