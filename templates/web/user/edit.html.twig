{% extends 'web/base.html.twig' %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('web/user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_index') }}">back to list</a>

    {{ include('web/user/_delete_form.html.twig') }}
{% endblock %}
