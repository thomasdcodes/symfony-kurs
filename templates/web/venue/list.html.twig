{% extends 'web/base.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="text-primary">Veranstaltungsorte</h1>

        <table class="table">
            <thead>
            <tr>
                <th>#</th>
                <th>Name des Ortes</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for venue in venues %}
                <tr>
                    <td>{{ venue.id }}</td>
                    <td>{{ venue.name }}</td>
                    <td>
                        <a href="{{ path('app_venue_show', {id: venue.id}) }}">Details</a>
                        <a href="{{ path('app_venue_edit', {id: venue.id}) }}">Editieren</a>
                        <a href="{{ path('app_venue_delete', {id: venue.id}) }}">Löschen</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}